version: '3.8'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

  test:
    build: .
    env_file:
      - .env
    command: pytest tests/
    profiles:
      - test
